openapi: 3.1.0
info:
  # Do not change the title, if the title changes, the import paths will be broken
  title: Api
  version: 0.1.0
  description: Travel Tracker API
servers:
  - url: /api
    description: Base API path
tags:
  - name: health
    description: Health operations
  - name: visits
    description: Travel visit operations
  - name: stats
    description: Statistics and summary operations
  - name: memories
    description: Per-country best memory and photo
paths:
  /healthz:
    get:
      operationId: healthCheck
      tags: [health]
      summary: Health check
      description: Returns server health status
      responses:
        "200":
          description: Healthy
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthStatus"

  /visits:
    get:
      operationId: listVisits
      tags: [visits]
      summary: List all visits
      parameters:
        - name: year
          in: query
          required: false
          schema:
            type: ["integer", "null"]
        - name: country
          in: query
          required: false
          schema:
            type: ["string", "null"]
      responses:
        "200":
          description: List of visits
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Visit"
    post:
      operationId: createVisit
      tags: [visits]
      summary: Record a new visit
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/VisitInput"
      responses:
        "201":
          description: Created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Visit"
        "400":
          description: Validation error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /visits/{id}:
    get:
      operationId: getVisit
      tags: [visits]
      summary: Get a single visit
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Visit details
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Visit"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    patch:
      operationId: updateVisit
      tags: [visits]
      summary: Update a visit
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/VisitUpdate"
      responses:
        "200":
          description: Updated visit
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Visit"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    delete:
      operationId: deleteVisit
      tags: [visits]
      summary: Delete a visit
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "204":
          description: Deleted successfully
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /stats/overview:
    get:
      operationId: getOverview
      tags: [stats]
      summary: Get overall travel stats
      responses:
        "200":
          description: Overview statistics
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Overview"

  /stats/years:
    get:
      operationId: getYearStats
      tags: [stats]
      summary: Get per-year breakdown of visits
      responses:
        "200":
          description: Year summaries
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/YearSummary"

  /memories:
    get:
      operationId: listMemories
      tags: [memories]
      summary: List all country memories
      responses:
        "200":
          description: All country memories
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/CountryMemory"

  /memories/{countryCode}:
    get:
      operationId: getMemory
      tags: [memories]
      summary: Get memory for a specific country
      parameters:
        - name: countryCode
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Country memory
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CountryMemory"
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
    put:
      operationId: upsertMemory
      tags: [memories]
      summary: Create or update memory for a country
      parameters:
        - name: countryCode
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CountryMemoryInput"
      responses:
        "200":
          description: Updated memory
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CountryMemory"
    delete:
      operationId: deleteMemory
      tags: [memories]
      summary: Delete a country memory
      parameters:
        - name: countryCode
          in: path
          required: true
          schema:
            type: string
      responses:
        "204":
          description: Deleted
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"

  /stats/timeline:
    get:
      operationId: getTimeline
      tags: [stats]
      summary: Get visits ordered chronologically for map animation
      responses:
        "200":
          description: Chronological visit list
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Visit"

components:
  schemas:
    HealthStatus:
      type: object
      properties:
        status:
          type: string
      required:
        - status

    Visit:
      type: object
      required: [id, country, countryCode, city, visitDate, lat, lng, createdAt]
      properties:
        id:
          type: integer
        country:
          type: string
        countryCode:
          type: string
          description: ISO 3166-1 alpha-2 code
        city:
          type: string
        visitDate:
          type: string
          format: date
        notes:
          type: ["string", "null"]
        lat:
          type: number
        lng:
          type: number
        createdAt:
          type: string
          format: date-time

    VisitInput:
      type: object
      required: [country, countryCode, city, visitDate, lat, lng]
      properties:
        country:
          type: string
          minLength: 1
        countryCode:
          type: string
          minLength: 2
          maxLength: 2
        city:
          type: string
          minLength: 1
        visitDate:
          type: string
          format: date
        notes:
          type: string
        lat:
          type: number
        lng:
          type: number

    VisitUpdate:
      type: object
      properties:
        country:
          type: string
          minLength: 1
        countryCode:
          type: string
          minLength: 2
          maxLength: 2
        city:
          type: string
          minLength: 1
        visitDate:
          type: string
          format: date
        notes:
          type: ["string", "null"]
        lat:
          type: number
        lng:
          type: number

    Overview:
      type: object
      required: [totalVisits, totalCountries, totalCities, topCountry, firstVisit, latestVisit]
      properties:
        totalVisits:
          type: integer
        totalCountries:
          type: integer
        totalCities:
          type: integer
        topCountry:
          type: ["string", "null"]
        firstVisit:
          type: ["string", "null"]
          format: date
        latestVisit:
          type: ["string", "null"]
          format: date

    YearSummary:
      type: object
      required: [year, countries, cities, visits]
      properties:
        year:
          type: integer
        countries:
          type: integer
        cities:
          type: integer
        visits:
          type: integer
        countryList:
          type: array
          items:
            type: string
        cityList:
          type: array
          items:
            type: string

    CountryMemory:
      type: object
      required: [id, countryCode, country, updatedAt]
      properties:
        id:
          type: integer
        countryCode:
          type: string
        country:
          type: string
        bestMemory:
          type: ["string", "null"]
        bestPhotoBase64:
          type: ["string", "null"]
        bestPhotoMime:
          type: ["string", "null"]
        updatedAt:
          type: string
          format: date-time

    CountryMemoryInput:
      type: object
      properties:
        bestMemory:
          type: ["string", "null"]
        bestPhotoBase64:
          type: ["string", "null"]
        bestPhotoMime:
          type: ["string", "null"]

    ErrorResponse:
      type: object
      required: [error]
      properties:
        error:
          type: string
